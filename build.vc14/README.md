The .sln files were upgraded from the build.vc11 directory with the following changes:

* the debug libraries target names are all suffuxed with `_d` to allow them to coexist with the release versions